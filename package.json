{
  "name": "aws-lambda-middlewares",
  "version": "1.0.2",
  "description": "Some middleware utils for composing lambda functions",
  "main": "lib/index.js",
  "types": "lib/index",
  "files": [
    "lib/"
  ],
  "scripts": {
    "prepublish": "npm run clean & npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run typescript:compile",
    "clean": "rm -Rf lib",
    "typescript:compile": "tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AitorGuerrero/aws-lambda-middlewares.git"
  },
  "keywords": [
    "aws",
    "lambda",
    "middleware"
  ],
  "author": "Aitor Guerrero",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/AitorGuerrero/aws-lambda-middlewares/issues"
  },
  "homepage": "https://github.com/AitorGuerrero/aws-lambda-middlewares#readme",
  "dependencies": {
    "typescript": "^2.2.1"
  }
}
